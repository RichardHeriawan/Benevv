export const SET_TEMPORAL = 'SET_TEMPORAL'

const SetTemporal = (payloadValue) => ({ type: SET_TEMPORAL, payloadValue })
const mapDispatchToProps = {
  SetTemporal
}

export default mapDispatchToProps
